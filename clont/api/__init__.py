"""Optional API uplink: ship the agent's per-cycle batch to a clont server and
dispatch the events it returns.

Inert unless an `api:` block is configured (free tier ships nothing). See
`uplink.ApiUplink` for the two-way contract.
"""
